/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // i18n: {
  //   locales: ['en', 'br'],
  //   defaultLocale: 'br',
  //   localDetection: false
  // }
}

module.exports = nextConfig
